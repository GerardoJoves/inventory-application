extends layout.pug

block content
  main
    div
      h1= title
      if genre
        a(href='/catalog/genres/update/' + genre.id) Edit Genere
      if developer
        a(href='/catalog/developers/update/' + developer.id) Edit Developer
    div
      include includes/searchBar.pug
    div
      include includes/gameList.pug
      
